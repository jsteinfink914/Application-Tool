{
    "name": "your-app-name",
    "version": "1.0.0",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "papaparse": "^5.3.0",
        "svelte": "^3.59.2",
        "vite": "^4.5.9"
    },
    "devDependencies": {
        "@sveltejs/vite-plugin-svelte": "^2.0.0"
    },
    "engines": {
        "node": "22.x"
    },
    "scripts": {
  "build": "npm install --unsafe-perm && vite build"
}
}
